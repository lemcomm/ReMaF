<div id="sd_content">
	<h2 id="sd_name">{% if char.knownAs %}{{ char.knownAs }} ({{ char.pureName }}){% else %}{{ char.pureName }}{% endif %} {% if char.male %}♂{% else %}♀{% endif %}</h2>
	<div id="sd_id" class="hidden">{{ char.id }}</div>
	<dl>
		{% for parent in char.parents %}
			<dt>
			{% if parent.male %}
				{{ 'character.father'|trans|title }}
			{% else %}
				{{ 'character.mother'|trans|title }}
			{% endif %}
			</dt>
			<dd>{{ link(parent) }}</dd>
		{% endfor %}
		<dt>{{ 'character.child'|trans({'count':char.children|length})|title }}</dt>
		<dd><ul class="simple">
			{% for child in char.children %}
				<li>{{ link(child) }}</li>
			{% else %}
				<li>{{ 'none'|trans }}</li>
			{% endfor %}
		</ul></dd>
		<hr />
		<dt>{{ 'character.details.realm'|trans }}</dt>
		<dd><ul class="simple">
			{% for realm in realms %}
				<li>{{ link(realm) }}</li>
			{% endfor %}
		</ul></dd>
		<dt>{{ 'character.details.ultimate'|trans }}</dt>
		<dd><ul class="simple">
			{% for realm in ultimates %}
				<li>{{ link(realm) }}</li>
			{% endfor %}
		</ul></dd>
		<br />
		<dt>{{ 'estate'|trans({'count':char.ownedSettlements|length})|title }}</dt>
		<dd><ul class="simple">
			{% for settlement in char.ownedSettlements %}
				<li>{{ link(settlement) }}</li>
			{% else %}
				<li>{{ 'none'|trans }}</li>
			{% endfor %}
		</ul></dd>
	</dl>
	<div class="clear" />
	<div id="sd_details" class="sd_button"><a href="{{ path("maf_char_view", {'id':char.id}) }}">{{ 'details'|trans|title }}</a></div>
</div>
