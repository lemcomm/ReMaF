{% block content %}
	<p>{{ "news.addintro"|trans({},"communication") }}</p>
	<form id="adder" class="wide" action="{{ path('maf_news_editor_add') }}" method="post">
		<input type="hidden" name="paper" value="{{ paperid }}"/>
		{{ form_widget(form) }}
	</form>
{% endblock %}
