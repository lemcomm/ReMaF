{% extends "layout.html.twig" %}

{% trans_default_domain 'actions' %}
{% block content %}
	<h2>{{ 'control.settle.name'|trans()|title }}</h2>
	<p>{{ 'control.settle.intro'|trans()|raw }}</p>
	{{ form(form) }}
{% endblock %}
