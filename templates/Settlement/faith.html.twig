{% extends "layout.html.twig" %}

{% block content %}
	<h2>{{ "assoc.route.faith.settlement.name"|trans({},"orgs")|title }}</h2>
	<p>{{ "assoc.route.faith.settlement.intro"|trans({},"orgs")|raw }}</p>
	{{ form(form) }}

{% endblock %}
