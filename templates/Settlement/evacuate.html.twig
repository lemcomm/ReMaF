{% extends "layout.html.twig" %}

{% block content %}
	<h2>{{ 'control.evacuate.name'|trans({"%settlement%":settlement.name},"actions")|title }}</h2>
	<p>{{ 'control.evacuate.help'|trans({"%settlement%":settlement.name},"actions")|raw }}</p>
	{{ form(form) }}
{% endblock %}
