{% extends "layout.html.twig" %}

{% trans_default_domain 'activity' %}
{% block content %}
	<h2>{{ "activity.join.name"|trans|title }}</h2>
	<p>{{ "activity.join.intro"|trans|raw}}</p>
	{{ form(form, {'attr': {'class':'wide'}}) }}

{% endblock %}
