{% extends "layout.html.twig" %}

{% block content %}
<h2>{{ 'unit.appoint.name'|trans({"%name%":unit.name},"actions")|title }}</h2>
<p>{{ 'unit.appoint.intro'|trans({"%name%":unit.name},"actions")|raw }}</p>
	{{ form(form) }}

{% endblock %}
