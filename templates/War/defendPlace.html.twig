{% extends "layout.html.twig" %}

{% block content %}
	<h2>{{ "military.place.defend.name"|trans({},"actions")|title }}</h2>
	{% if result is defined %}
		<p>
		{% if result.success %}
			{% if result.immediate %}
				{{ "military.place.defend.success"|trans({}, "actions")|raw }}
			{% else %}
				{{ "military.place.defend.queued"|trans({}, "actions")|raw }}
			{% endif %}
		{% else %}
			{{ result.message|trans({},"actions") }}
		{% endif %}
		</p>
	{% else %}
		<p>{{ "military.place.defend.intro"|trans({"%settlement%":link(place)},"actions")|raw }}</p>
		{{ form(form) }}
	{% endif %}

{% endblock %}
